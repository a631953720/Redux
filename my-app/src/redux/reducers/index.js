import { combineReducers } from "redux";
// import {reducer} from './reducer';
import {device} from './device';
// export default combineReducers({reducer});
export default combineReducers({deviceList:device});
